import React from "react";

const Yaya = (props) => {
    return(
        <div className="f2 tc karate">            
            <h1>{props.msg}</h1>
        </div>
    );
}      
export default Yaya;